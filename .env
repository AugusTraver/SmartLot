# URL del backend API (mismo valor que PORT en .env del backend)
VITE_API_URL=http://localhost:3000

# Token lifetime (debe coincidir con JWT_EXPIRES_IN del backend)
VITE_JWT_EXPIRES_IN=2h

# Clave en sessionStorage para el JWT
VITE_TOKEN_KEY=smartlot_token
